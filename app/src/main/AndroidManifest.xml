<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rivaldo.pemesananonlinetokojagokupang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PemesananOnlineTokoJagoKupang">
        <activity android:name=".activity.DeveloperProfileActivity"></activity>
        <activity android:name=".activity.PembayaranActivity" />
        <activity android:name=".activity.KeranjangActivity" />
        <activity android:name=".activity.ListBarang" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>